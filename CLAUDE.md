# 프로그래머스 코딩테스트 대비 지침

이 문서는 이 저장소에서 프로그래머스(programmers.co.kr) 문제를 풀 때 따라야 할 작업 프로세스를 정의한다. 목적은 실제 코딩테스트 환경을 가정하고, 매번 동일한 절차로 문제를 분석·풀이·검증함으로써 실전에서도 재현 가능한 습관을 만드는 것이다.

## 0. 언어 제한

- 풀이 언어는 **C++로 제한**한다. 다른 언어(Python/JS 등)로 풀이하지 않는다.

## 1. 문제 입력 받기

문제를 풀기 전, 사용자로부터 아래 4가지를 모두 전달받는다.

1. 문제 레벨 (Level 1 / 2 / 3 ...)
2. 문제 제목
3. 문제 내용 전문 (설명 + 제한사항 포함)
4. 최초 소스 (프로그래머스가 제공하는 함수 시그니처/템플릿 코드)

이 중 하나라도 빠져 있으면 임의로 추측하거나 생략하지 말고, 먼저 사용자에게 요청하여 받은 뒤에 풀이를 시작한다.

## 2. 제한사항(조건) 정리 — 빠짐없이 확인

코드를 작성하기 전에 문제의 제한사항을 항목별로 다시 정리한다. 다음을 빠짐없이 체크한다.

- 입력 값/배열/문자열의 길이 범위
- 각 값의 최솟값/최댓값 범위
- 입력 크기로 유추되는 시간복잡도 제약 (예: N ≤ 100,000 → O(N log N) 이하 필요)
- 특수 조건 (예: 소문자만 존재, 중복 없음, 정렬 여부, 특정 문자만 포함 등)
- 출력 형식 (반환 타입, 배열/정수/문자열 여부, 소수점 처리 등)

이 체크리스트를 정리한 뒤에만 코드 작성으로 넘어간다.

## 3. Edge Case 도출

2번에서 정리한 제한사항을 근거로, 문제에 직접 제시되지 않은 엣지 케이스를 모두 나열한다. 최소한 아래 항목을 검토한다.

- 입력 최솟값 / 최댓값 경계
- 원소가 1개뿐인 경우
- 허용된다면 빈 입력
- 모든 값이 동일한 경우
- 중복 값이 있는 경우
- 음수/0 포함 여부 (문제에서 허용하는 경우)
- 오버플로 가능성 (C++는 int 범위 주의 — 필요 시 long long 사용)
- 정렬 상태에 따라 결과가 달라지는지 여부

여기서 도출한 케이스는 6번 자체 테스트케이스 설계에 반영한다.

## 4. 코드 작성 원칙

- **시그니처 고정**: 최초로 주어진 함수의 이름·매개변수·반환 타입을 절대 변경하지 않는다.
- **로직 구현 원칙**: 기본적으로 별도의 사용자 정의 함수(헬퍼 함수, 클래스 등)는 최대한 만들지 않는다. 주어진 `solution` 함수 내부에서 논리적 흐름을 따라 순차적으로 문제를 해결한다. 단, `<algorithm>`의 `sort`, `min`, `max`, `accumulate` 등 표준 라이브러리 함수 호출은 사용자 정의 함수 분리가 아니므로 허용한다. 문제 난이도상 로직 분리가 불가피한 경우, `solution` 내부에 익명 함수(람다)로 구현하는 것을 우선 고려하되, 그마저도 불가피하다면 `solution` 외부에 별도의 함수를 작성하여 사용할 수 있다.
- **단순한 로직 우선, 단 수학적 축약 허용**: 실전 시험에서 외워서 그대로 재현할 수 있는 수준의 직관적인 반복문/조건문을 우선한다. 다만 수학적 계산(공식, 점화식 등)으로 로직을 명확히 단순화할 수 있는 경우에는 축약된 코드를 작성할 수 있다. 근거 없이 과도하게 압축된 표현식이나 트릭성 코드(예: 불필요한 삼항연산자 중첩, 비트마스크 남용)는 지양한다.
- **최소 include**: 실제로 사용하는 헤더만 포함한다. `<bits/stdc++.h>` 같은 전체 포함은 사용하지 않는다.

## 5. 자체 테스트케이스(TC) 설계 및 실행

- 문제 설명에 나와 있지 않은 자체 TC를 **최소 5개 이상** 설계한다.
- 3번에서 도출한 엣지 케이스를 우선적으로 반영한다.
- 각 TC에 대해 입력값과 기대 출력을 먼저 정하고, 작성한 코드로 실제 출력을 확인하여 일치 여부를 검증한다.
- 불일치가 발견되면 `solution` 함수 내부 로직만 수정하여 디버깅한다 (시그니처/구조는 유지).

## 6. 결과물 저장 규칙

문제 풀이가 끝나면 아래 3가지를 저장/갱신한다. 파일명은 문제 제목의 공백을 언더스코어(`_`)로 치환한 형태를 사용한다 (기존 저장소 관례 유지).

1. **소스 코드**: `Level_N/문제_제목.cpp`
2. **자체 TC 기록**: `Level_N/문제_제목_TC.md` — 소스 파일과 같은 폴더에, 아래 표 형식으로 5번에서 실행한 TC를 정리한다.

   ```markdown
   # 문제 제목 - 자체 테스트케이스

   | # | 입력 | 기대 출력 | 실제 출력 | 결과 |
   |---|------|-----------|-----------|------|
   | 1 | ... | ... | ... | Pass |
   | 2 | ... | ... | ... | Pass |
   | 3 | ... | ... | ... | Pass |
   | 4 | ... | ... | ... | Pass |
   | 5 | ... | ... | ... | Pass |
   ```

3. **README.md 갱신**: 저장소 루트 `README.md`의 해당 자음/문자 섹션(`## ㄱ`, `## ㄴ` ... `## Else`)에, 가나다순 위치를 지켜 아래 형식으로 항목을 추가한다.

   ```
   - ### <a href="문제 URL" target="_blank" rel="noopener">문제 제목</a> ✅ - Level N
   ```

   풀이가 아직 통과하지 못했다면 ✅ 대신 ❗️를 사용하고, 실패 사유를 뒤에 짧게 덧붙인다 (예: `- Level 2 - 효율성 실패`).

## 7. 작업 순서 요약

1. 문제 레벨/제목/내용/최초 소스 전달받기
2. 제한사항 체크리스트 정리
3. Edge Case 목록 도출
4. 최초 소스 시그니처를 유지하며 단순한 로직으로 C++ 코드 작성 (최소 include, 별도 함수 최소화)
5. 자체 TC 5개 이상 설계 및 실행 → 결과 검증
6. `Level_N/문제_제목.cpp`, `Level_N/문제_제목_TC.md` 저장
7. `README.md` 체크리스트 갱신
